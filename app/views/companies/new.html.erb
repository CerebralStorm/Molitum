<div class="row-fluid">

  <div class="span8 form-highlight round padding2">
    <h2>Register a company for <%= current_user.name %></h2>
    <div class="line"></div>
    <span class="required_notification">* Denotes Required Field</span>
    <%= simple_form_for(@company) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs">
        <%= f.input :name, :autofocus => true, required: false, input_html: { required: "required"}  %>
        <%= f.input :website %>
        <%= f.input :address %>
        <%= f.input :city %>
        <%= f.input :state %>
        <%= f.input :zip %>
      </div>

      <div class="form-actions">
        <button class="btn btn-primary btn-large" name="commit" type="submit" value="Create User">Submit</button>
      </div>
    <% end %>
  </div>

  <div class="span4 border round highlight ">
    <h2>Now You're On Track</h2>
    <h6>Time saving starts now</h6>
    </br>
    <h3> Step 2: Optional Information</h3>
    <p>After you have registerd a company, you can begin to keep track off all the companies profits and losses.</p>
  </div>

</div>
