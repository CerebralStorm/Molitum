/* === Remove input autofocus webkit === */
*:focus {outline: none;}

/* === Form Typography === */
.help-block, .required_notification {font-size: 11px;}

/* === Form Header === */
.contact_form h2 {
  margin:0;
  display: inline;
}
.required_notification {
  color:#d45252; 
  margin:5px 0 0 0; 
  display:inline;
  float:right;
}

.simple_form textarea {padding:8px; width:300px;}

/* form element visual styles */
.simple_form input[type="text"], .simple_form input[type="email"], .simple_form input[type="tel"], .simple_form textarea, 
.simple_form input[type="number"] { 
  border:1px solid #aaa;
  box-shadow: 0px 0px 3px #ccc, 0 10px 15px #eee inset;
  border-radius:2px;
  padding-right:30px;
  -moz-transition: padding .25s; 
  -webkit-transition: padding .25s; 
  -o-transition: padding .25s;
  transition: padding .25s;
}

.simple_form input:focus, .simple_form textarea:focus {
  background: #fff; 
  border:1px solid #555; 
  box-shadow: 0 0 3px #aaa; 
  padding-right:70px;
}

.simple_form select {
   width:250px;
   *width: 250px;
   font-size:20px; 
  padding:2px 10px; 

  -webkit-appearance: none; 
}

/* === HTML5 validation styles === */ 
.simple_form input:required, .simple_form textarea:required, .simple_form select:required {
  background: #fff url('<%= asset_path 'red_asterisk.png' %>') no-repeat 98% center;
  box-shadow: 1px 1px 11px #d45252;
}
.simple_form input:required:valid, .simple_form textarea:required:valid {
  background: #fff url('<%= asset_path 'valid.png' %>') no-repeat 98% center;
  box-shadow: 0 0 5px #5cd053;
  border-color: #28921f;
}
.simple_form input:focus:invalid, .simple_form textarea:focus:invalid {
  background: #fff url('<%= asset_path 'invalid.png' %>') no-repeat 98% center;
  box-shadow: 0 0 5px #d45252;
  border-color: #b03535
}

.simple_form select:required:valid {
  color: #000;
  background: #fff url('<%= asset_path 'valid.png' %>') no-repeat 80% center;
  box-shadow: 0 0 5px #5cd053;
  border-color: #28921f;
}
.simple_form select:focus:invalid {
  background: url('<%= asset_path 'invalid.png' %>') no-repeat 80% center;
  box-shadow: 0 0 5px #d45252;
  *background: #fff;
}

/* === Form hints === */
.help-block {
  background: #d45252;
  border-radius: 3px 3px 3px 3px;
  color: white;
  margin-left:8px;
  padding: 1px 6px;
  z-index: 999; /* hints stay above all other elements */
  position: absolute; /* allows proper formatting if hint is two lines */
  display: none;
}
.help-block::before {
  content: "\25C0";
  color:#d45252;
  position: absolute;
  top:1px;
  left:-6px;
}
.simple_form input:focus + .help-block {display: inline;}
.simple_form input:required:valid + .help-block {background: #28921f;}
.simple_form input:required:valid + .help-block::before {color:#28921f;}




